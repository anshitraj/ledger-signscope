/**
 * DEPRECATED — generateAgentTransaction is now handled by geminiGenerateTransaction()
 * in services/gemini.ts and called directly from routes/requestCheck.ts and
 * routes/signscope.ts (via /api/agent/generate-transaction).
 *
 * This file is kept as a stub to prevent import errors from tests.
 * It is NOT used in the main pipeline.
 */

export {};
